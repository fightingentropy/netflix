{
  "name": "netflix-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --hot server.js",
    "dev:vite": "vite --host localhost --port 5173 --strictPort",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
